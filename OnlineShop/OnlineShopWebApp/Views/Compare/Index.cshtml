@model List<Product>;

@if (Model.Count() == 0)
{
    <div class="text-center">
        <h3 class="bg-warning-subtle text-emphasis-warning">В списке сравнения еще нет товаров</h3>
        <a class="btn btn-primary" asp-controller="home" asp-action="index">Перейти к покупкам</a>
    </div>
}
else
{
    <h4>Сравнение</h4>
    <div class="row">
        <div class="col">
            <table class="table" style="font-size:15px">
                <thead>
                    <tr>
                        <th>Название</th>                        
                        <th>Интерфейс</th>
                        <th>Частота графического процессора</th>
                        <th>Частота графического процессора (Boost)</th>
                        <th>Объём памяти</th>
                        <th>Тип памяти</th>
                        <th>Частота видеопамяти</th>
                        <th>Шина памяти (разрядность)</th>
                        <th>Разъёмы</th>
                        <th>Цена</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var product in Model)
                    {
                        <tr>
                            <td class="col-4"><a style="color:navy" asp-controller="product" asp-action="index" asp-route-id="@product.Id"><p>@product.Name</p></a></td>                            
                            <td>@product.Interface</td>
                            <td>@product.FrequencyGPU</td>
                            <td>@product.FrequencyGPUBoost</td>
                            <td>@product.MemoryCapacity</td>
                            <td>@product.MemoryType</td>
                            <td>@product.FrequencyMemory</td>
                            <td>@product.MemoryBus</td>
                            <td>@product.Connectors</td>                            
                            <td class="col-3">@string.Format("{0:### ### ###}", product.Cost) р</td>
                            <td class="col-3 text-end">
                                <a class="btn btn-outline-warning" title="Удалить из списка" asp-controller="compare" asp-action="del" asp-route-id="@product.Id"><i class="bi bi-trash-fill"></i></a>
                                <a class="btn btn-warning" title="Добавить в корзину" asp-controller="cart" asp-action="add" asp-route-productid="@product.Id"> <i class="bi bi-cart"></i> </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>

            <div class="text-center">
                <a class="btn btn-primary" asp-controller="home" asp-action="index"><i class="bi bi-arrow-90deg-left"> Продолжить покупки</i></a>
                <a class="btn btn-outline-primary" asp-controller="compare" asp-action="clear"><i class="bi bi-x-circle"> Очистить список</i></a>
            </div>
        </div>
    </div>
}